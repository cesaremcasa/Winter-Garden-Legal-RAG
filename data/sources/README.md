# Operator sources

Place PDFs or HTML files here only when you have permission to process them.
This directory is intentionally empty in the public repository. Run
`python scripts/build_index.py` after adding sources.
